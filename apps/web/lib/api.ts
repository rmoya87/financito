let csrfToken:string|null=null; let sessionPromise:Promise<string>|null=null;
async function ensureSession():Promise<string>{if(csrfToken)return csrfToken;if(!sessionPromise){sessionPromise=fetch('/api/v1/session',{credentials:'same-origin'}).then(async res=>{if(!res.ok)throw new Error('No se pudo iniciar la sesión local');const data=await res.json() as {csrf_token:string};csrfToken=data.csrf_token;return data.csrf_token}).finally(()=>{sessionPromise=null})}return sessionPromise}
async function parse<T>(res:Response):Promise<T>{if(!res.ok){const text=await res.text();throw new Error(text||`Error ${res.status}`)}return res.json() as Promise<T>}
export async function apiGet<T>(path:string):Promise<T>{await ensureSession();return parse<T>(await fetch(path,{credentials:'same-origin',cache:'no-store'}))}
export async function apiMutate<T>(path:string,method:'POST'|'PATCH'|'DELETE',body?:unknown):Promise<T>{const csrf=await ensureSession();return parse<T>(await fetch(path,{method,credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':csrf},body:body===undefined?undefined:JSON.stringify(body)}))}
export async function apiUpload<T>(path:string,form:FormData):Promise<T>{const csrf=await ensureSession();return parse<T>(await fetch(path,{method:'POST',credentials:'same-origin',headers:{'X-CSRF-Token':csrf},body:form}))}
