'use client';
import {FormEvent,useState} from 'react';import {useMutation,useQuery} from '@tanstack/react-query';import {apiGet,apiMutate} from '@/lib/api';import {PageHeader} from '@/components/page-header';import {Card} from '@/components/ui/card';import {Money} from '@/components/ui/money';type Estimate={jurisdiction:string;tax_year:number;realized_pnl:string;estimated_tax:string|null;assumed_rate:string|null;status:string;notice:string};type InsightItem={title:string;detail:string;pages:number[];impact?:string};type DocInsight={document_id:string;file_name:string;analysis:{summary:string;optimization_opportunities:InsightItem[];missing_information:InsightItem[];comparison_requirements:InsightItem[];risks:InsightItem[]}};
export default function TaxPage(){const insights=useQuery({queryKey:['document-insights','tax'],queryFn:()=>apiGet<DocInsight[]>('/api/v1/document-insights?document_type=tax')});const [year,setYear]=useState(String(new Date().getFullYear()));const [rate,setRate]=useState('');const q=useMutation({mutationFn:()=>apiMutate<Estimate>('/api/v1/tax/estimate','POST',{jurisdiction:'ES',tax_year:Number(year),assumed_rate:rate===''?null:rate})});return <><PageHeader title="Tax Center" description="Recopila P&L realizado desde lotes. No aplica una normativa fiscal inventada: hasta instalar un módulo normativo versionado, la tasa debe introducirse expresamente."/><Card><form className="grid gap-2 sm:grid-cols-3" onSubmit={(e:FormEvent)=>{e.preventDefault();q.mutate()}}><input className="fin-input" type="number" value={year} onChange={e=>setYear(e.target.value)}/><input className="fin-input" placeholder="Tasa asumida decimal (opcional)" value={rate} onChange={e=>setRate(e.target.value)}/><button className="fin-button">Calcular</button></form>{q.data&&<div className="mt-5"><div className="text-sm text-[var(--muted)]">P&L realizado</div><div className="text-2xl font-bold"><Money value={q.data.realized_pnl}/></div><div className="mt-3 text-sm">{q.data.estimated_tax===null?'Sin estimación de impuesto: falta una tasa explícita.':'Impuesto estimado: '+q.data.estimated_tax+' €'}</div><div className="mt-2 text-xs text-[var(--muted)]">{q.data.notice}</div></div>}</Card><Card className="mt-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-bold">Conclusiones de documentación fiscal</h2><p className="mt-1 text-sm text-[var(--muted)]">La IA local resume únicamente datos y condiciones visibles. No convierte una interpretación en regla fiscal automática.</p></div><a className="text-xs underline" href="/documents/">Añadir documentos</a></div><div className="mt-4 grid gap-3 md:grid-cols-2">{insights.data?.length?insights.data.map(x=><div key={x.document_id} className="rounded-xl bg-[var(--surface-2)] p-3 text-sm"><div className="flex items-start justify-between gap-3"><strong>{x.file_name}</strong><a className="text-xs underline" href={'/documents/?document='+encodeURIComponent(x.document_id)}>Evidencia</a></div><p className="mt-2 text-xs">{x.analysis.summary}</p>{x.analysis.optimization_opportunities?.length>0&&<div className="mt-2 rounded-lg bg-[var(--brand-soft)] p-2 text-xs"><strong>A revisar:</strong> {x.analysis.optimization_opportunities.slice(0,2).map(i=>i.title||i.detail).join(' · ')}</div>}{x.analysis.missing_information?.length>0&&<div className="mt-2 text-xs"><strong>Falta:</strong> {x.analysis.missing_information.slice(0,2).map(i=>i.title||i.detail).join(' · ')}</div>}</div>):<div className="text-sm text-[var(--muted)]">Añade declaraciones, certificados o documentación fiscal para disponer de contexto local.</div>}</div></Card></>}
