'use client';
import {FormEvent,useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {apiGet,apiMutate} from '@/lib/api';import {PageHeader} from '@/components/page-header';import {Card} from '@/components/ui/card';import {Money} from '@/components/ui/money';import {EmptyState,ErrorState,Loading} from '@/components/ui/states';
type Account={id:string;name:string;institution_name:string;account_type:string;currency:string;current_balance:string;available_balance:string|null;sync_status:string};
export default function AccountsPage(){const qc=useQueryClient();const query=useQuery({queryKey:['accounts'],queryFn:()=>apiGet<Account[]>('/api/v1/accounts')});const [name,setName]=useState('');const [institution,setInstitution]=useState('');const [balance,setBalance]=useState('0');const create=useMutation({mutationFn:()=>apiMutate<Account>('/api/v1/accounts','POST',{name,institution_name:institution||'Manual',current_balance:balance,currency:'EUR'}),onSuccess:()=>{setName('');setInstitution('');setBalance('0');qc.invalidateQueries({queryKey:['accounts']});qc.invalidateQueries({queryKey:['dashboard']})}});function submit(e:FormEvent){e.preventDefault();create.mutate()}return <><PageHeader title="Cuentas" description="Añade cuentas manualmente o impórtalas mediante los conectores de solo lectura cuando estén configurados."/><div className="grid gap-4 xl:grid-cols-[1fr_360px]"><Card><h2 className="font-bold">Tus cuentas</h2><div className="mt-4 space-y-3">{query.isLoading?<Loading/>:query.error?<ErrorState error={query.error}/>:query.data?.length?query.data.map(a=><div key={a.id} className="flex items-center justify-between rounded-xl bg-[var(--surface-2)] p-4"><div><div className="font-semibold">{a.name}</div><div className="text-xs text-[var(--muted)]">{a.institution_name} · {a.sync_status}</div></div><div className="text-right font-bold"><Money value={a.current_balance} currency={a.currency}/></div></div>):<EmptyState>Aún no hay cuentas. Crea la primera para empezar.</EmptyState>}</div></Card><Card><h2 className="font-bold">Nueva cuenta</h2><form onSubmit={submit} className="mt-4 space-y-3"><label className="block text-sm">Nombre<input className="fin-input mt-1" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="block text-sm">Entidad<input className="fin-input mt-1" value={institution} onChange={e=>setInstitution(e.target.value)} placeholder="Ej. Bankinter"/></label><label className="block text-sm">Saldo actual<input className="fin-input mt-1" type="number" step="0.01" value={balance} onChange={e=>setBalance(e.target.value)} required/></label><button className="fin-button w-full" disabled={create.isPending}>Guardar cuenta</button>{create.error&&<ErrorState error={create.error}/>}</form></Card></div></>}
