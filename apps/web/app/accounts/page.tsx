'use client';
import Link from 'next/link';
import {FormEvent,useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {apiGet,apiMutate} from '@/lib/api';import {PageHeader} from '@/components/page-header';import {Card} from '@/components/ui/card';import {Money} from '@/components/ui/money';import {EmptyState,ErrorState,Loading} from '@/components/ui/states';
type Account={id:string;name:string;institution_name:string;account_type:string;currency:string;current_balance:string;available_balance:string|null;sync_status:string};type InsightItem={title:string;detail:string;pages:number[];impact?:string};type DocInsight={document_id:string;file_name:string;analysis:{summary:string;optimization_opportunities:InsightItem[];negotiation_points:InsightItem[];comparison_requirements:InsightItem[];risks:InsightItem[]}};
export default function AccountsPage(){const qc=useQueryClient();const query=useQuery({queryKey:['accounts'],queryFn:()=>apiGet<Account[]>('/api/v1/accounts')});const insights=useQuery({queryKey:['document-insights','bank_statement'],queryFn:()=>apiGet<DocInsight[]>('/api/v1/document-insights?document_type=bank_statement')});const [name,setName]=useState('');const [institution,setInstitution]=useState('');const [balance,setBalance]=useState('0');const create=useMutation({mutationFn:()=>apiMutate<Account>('/api/v1/accounts','POST',{name,institution_name:institution||'Manual',current_balance:balance,currency:'EUR'}),onSuccess:()=>{setName('');setInstitution('');setBalance('0');qc.invalidateQueries({queryKey:['accounts']});qc.invalidateQueries({queryKey:['dashboard']})}});function submit(e:FormEvent){e.preventDefault();create.mutate()}return <><PageHeader title="Cuentas" description="Tus saldos y fuentes bancarias en un único lugar."/><div className="mb-4 flex flex-wrap gap-2"><Link href="/banking/" className="fin-button">Conectar banco</Link></div><div className="grid gap-4 xl:grid-cols-[1fr_360px]"><Card><h2 className="font-bold">Tus cuentas</h2><div className="mt-4 space-y-3">{query.isLoading?<Loading/>:query.error?<ErrorState error={query.error}/>:query.data?.length?query.data.map(a=><div key={a.id} className="flex items-center justify-between rounded-xl bg-[var(--surface-2)] p-4"><div><div className="font-semibold">{a.name}</div><div className="text-xs text-[var(--muted)]">{a.institution_name} · {a.sync_status==='synced'?'Sincronizada':a.sync_status==='pending_sync'?'Pendiente de sincronizar':a.sync_status==='disconnected'?'Desconectada':'Manual'}</div></div><div className="text-right font-bold">{a.sync_status==='pending_sync'?<span className="text-sm text-[var(--muted)]">Saldo pendiente</span>:<Money value={a.current_balance} currency={a.currency}/>}</div></div>):<EmptyState>Aún no hay cuentas. Crea la primera para empezar.</EmptyState>}</div></Card><Card><h2 className="font-bold">Nueva cuenta</h2><form onSubmit={submit} className="mt-4 space-y-3"><label className="block text-sm">Nombre<input className="fin-input mt-1" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="block text-sm">Entidad<input className="fin-input mt-1" value={institution} onChange={e=>setInstitution(e.target.value)} placeholder="Ej. Bankinter"/></label><label className="block text-sm">Saldo actual<input className="fin-input mt-1" type="number" step="0.01" value={balance} onChange={e=>setBalance(e.target.value)} required/></label><button className="fin-button w-full" disabled={create.isPending}>Guardar cuenta</button>{create.error&&<ErrorState error={create.error}/>}</form></Card></div><Card className="mt-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-bold">Costes y condiciones detectadas en extractos</h2><p className="mt-1 text-sm text-[var(--muted)]">La IA local puede señalar comisiones y condiciones visibles en documentos bancarios para ayudarte a comparar cuentas.</p></div><Link className="text-xs underline" href="/documents/">Añadir documentos</Link></div><div className="mt-4 grid gap-3 md:grid-cols-2">{insights.data?.length?insights.data.map(x=><div key={x.document_id} className="rounded-xl bg-[var(--surface-2)] p-3 text-sm"><div className="flex items-start justify-between gap-3"><strong>{x.file_name}</strong><Link className="text-xs underline" href={'/documents/?document='+encodeURIComponent(x.document_id)}>Evidencia</Link></div><p className="mt-2 text-xs">{x.analysis.summary}</p>{x.analysis.optimization_opportunities?.length>0&&<div className="mt-2 rounded-lg bg-[var(--brand-soft)] p-2 text-xs"><strong>Oportunidad:</strong> {x.analysis.optimization_opportunities.slice(0,2).map(i=>i.title||i.detail).join(' · ')}</div>}{x.analysis.negotiation_points?.length>0&&<div className="mt-2 text-xs"><strong>Para negociar:</strong> {x.analysis.negotiation_points.slice(0,2).map(i=>i.title||i.detail).join(' · ')}</div>}</div>):<EmptyState>Añade extractos o contratos bancarios si quieres revisar costes y condiciones.</EmptyState>}</div></Card></>}
