'use client';
import {FormEvent,useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {apiGet,apiMutate} from '@/lib/api';import {PageHeader} from '@/components/page-header';import {Card} from '@/components/ui/card';import {Money} from '@/components/ui/money';import {EmptyState,ErrorState} from '@/components/ui/states';
type Position={security_id:string;quantity:string;average_cost:string;price:string;value:string;unrealized_pnl:string};type P={id:string;name:string;base_currency:string;market_value:string;cost_basis:string;unrealized_pnl:string;positions:Position[]};type S={id:string;name:string;symbol:string|null;asset_class:string;currency:string};type Exposure={portfolio_id:string;market_value:string;by_asset_class:{asset_class:string;value:string;weight:string}[];positions:{security_id:string;name:string;symbol:string|null;value:string;weight:string}[];concentration_hhi:string;largest_position_weight:string};
export default function InvestmentsPage(){const qc=useQueryClient();const ps=useQuery({queryKey:['portfolios'],queryFn:()=>apiGet<P[]>('/api/v1/portfolios')});const ss=useQuery({queryKey:['securities'],queryFn:()=>apiGet<S[]>('/api/v1/securities')});const [pn,setPn]=useState('Principal');const [sec,setSec]=useState({name:'',symbol:'',asset_class:'stock'});const [trade,setTrade]=useState({portfolio_id:'',security_id:'',side:'buy',quantity:'',price:'',fees:'0',executed_at:new Date().toISOString().slice(0,16)});const [selectedPortfolio,setSelectedPortfolio]=useState('');const exposure=useQuery({queryKey:['portfolio-exposure',selectedPortfolio],queryFn:()=>apiGet<Exposure>('/api/v1/portfolios/'+selectedPortfolio+'/exposure'),enabled:!!selectedPortfolio});const addP=useMutation({mutationFn:()=>apiMutate('/api/v1/portfolios','POST',{name:pn,base_currency:'EUR'}),onSuccess:()=>qc.invalidateQueries({queryKey:['portfolios']})});const addS=useMutation({mutationFn:()=>apiMutate('/api/v1/securities','POST',{...sec,currency:'EUR',isin:null}),onSuccess:()=>qc.invalidateQueries({queryKey:['securities']})});const addT=useMutation({mutationFn:()=>apiMutate('/api/v1/trades','POST',{...trade,fx_rate:'1',currency:'EUR',executed_at:new Date(trade.executed_at).toISOString()}),onSuccess:()=>{qc.invalidateQueries({queryKey:['portfolios']});qc.invalidateQueries({queryKey:['portfolio-exposure',trade.portfolio_id]})}});const refresh=useMutation({mutationFn:(securityId:string)=>apiMutate('/api/v1/market/security/'+securityId+'/refresh?include_history=true','POST'),onSuccess:()=>{qc.invalidateQueries({queryKey:['portfolios']});qc.invalidateQueries({queryKey:['portfolio-exposure']})}});return <><PageHeader title="Inversiones" description="Carteras, posiciones y lotes FIFO. La valoración de mercado solo se actualiza cuando la solicitas y conserva proveedor/frescura localmente."/><div className="grid gap-4 xl:grid-cols-3"><Card><h2 className="font-bold">Cartera</h2><form className="mt-3 flex gap-2" onSubmit={(e:FormEvent)=>{e.preventDefault();addP.mutate()}}><input className="fin-input" value={pn} onChange={e=>setPn(e.target.value)}/><button className="fin-button">Crear</button></form></Card><Card><h2 className="font-bold">Activo</h2><form className="mt-3 space-y-2" onSubmit={(e:FormEvent)=>{e.preventDefault();addS.mutate()}}><input className="fin-input" placeholder="Nombre" value={sec.name} onChange={e=>setSec({...sec,name:e.target.value})}/><input className="fin-input" placeholder="Ticker" value={sec.symbol} onChange={e=>setSec({...sec,symbol:e.target.value})}/><select className="fin-input" value={sec.asset_class} onChange={e=>setSec({...sec,asset_class:e.target.value})}><option value="stock">Acción</option><option value="etf">ETF</option><option value="fund">Fondo</option><option value="bond">Bono</option><option value="crypto">Cripto</option><option value="cash">Liquidez</option></select><button className="fin-button w-full">Añadir</button></form></Card><Card><h2 className="font-bold">Operación</h2><form className="mt-3 space-y-2" onSubmit={(e:FormEvent)=>{e.preventDefault();addT.mutate()}}><select className="fin-input" value={trade.portfolio_id} onChange={e=>setTrade({...trade,portfolio_id:e.target.value})}><option value="">Cartera…</option>{ps.data?.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select className="fin-input" value={trade.security_id} onChange={e=>setTrade({...trade,security_id:e.target.value})}><option value="">Activo…</option>{ss.data?.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><div className="grid grid-cols-2 gap-2"><select className="fin-input" value={trade.side} onChange={e=>setTrade({...trade,side:e.target.value})}><option value="buy">Compra</option><option value="sell">Venta</option></select><input className="fin-input" placeholder="Cantidad" value={trade.quantity} onChange={e=>setTrade({...trade,quantity:e.target.value})}/></div><input className="fin-input" placeholder="Precio" value={trade.price} onChange={e=>setTrade({...trade,price:e.target.value})}/><input className="fin-input" type="datetime-local" value={trade.executed_at} onChange={e=>setTrade({...trade,executed_at:e.target.value})}/><button className="fin-button w-full">Registrar</button></form></Card></div>{refresh.error&&<div className="mt-4"><ErrorState error={refresh.error}/></div>}<div className="mt-4 space-y-4">{ps.data?.length?ps.data.map(p=><Card key={p.id}><div className="flex flex-wrap justify-between gap-3"><div><div className="font-bold">{p.name}</div><div className="text-sm text-[var(--muted)]">Coste <Money value={p.cost_basis}/> · P&L <Money value={p.unrealized_pnl}/></div></div><div className="text-xl font-bold"><Money value={p.market_value}/></div></div><div className="mt-4 space-y-2">{p.positions.map(x=>{const s=ss.data?.find(s=>s.id===x.security_id);return <div key={x.security_id} className="flex flex-wrap items-center justify-between gap-2 rounded-xl bg-[var(--surface-2)] p-3 text-sm"><span>{s?.name||x.security_id}{s?.symbol?' · '+s.symbol:''}</span><span>{x.quantity} · <Money value={x.value}/></span>{s?.symbol&&<button className="text-xs underline" onClick={()=>refresh.mutate(x.security_id)}>Actualizar mercado</button>}</div>})}</div><button className="fin-button secondary mt-3" onClick={()=>setSelectedPortfolio(p.id)}>Analizar exposición</button>{selectedPortfolio===p.id&&exposure.data&&<div className="mt-4 rounded-xl border border-[var(--border)] p-4"><div className="text-sm font-semibold">Concentración</div><div className="mt-1 text-xs text-[var(--muted)]">Mayor posición {(Number(exposure.data.largest_position_weight)*100).toFixed(1)}% · HHI {Number(exposure.data.concentration_hhi).toFixed(3)}</div><div className="mt-3 space-y-1">{exposure.data.by_asset_class.map(x=><div key={x.asset_class} className="flex justify-between text-sm"><span>{x.asset_class}</span><span>{(Number(x.weight)*100).toFixed(1)}%</span></div>)}</div></div>}</Card>):<EmptyState>Crea una cartera para empezar.</EmptyState>}</div></>}
