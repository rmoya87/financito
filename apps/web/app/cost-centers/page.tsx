'use client';
import {FormEvent,useState} from 'react';import {useMutation,useQuery,useQueryClient} from '@tanstack/react-query';import {apiGet,apiMutate} from '@/lib/api';import {PageHeader} from '@/components/page-header';import {Card} from '@/components/ui/card';import {Money} from '@/components/ui/money';import {EmptyState,ErrorState} from '@/components/ui/states';
type Center={id:string;name:string;type:string;parent_id:string|null;links:{entity_type:string;entity_id:string;allocation_percentage:string}[]};type Summary={id:string;name:string;observed_linked_spend:string;annual_linked_commitments:string;reference_asset_value:string;reference_debt:string;unpriced_links:any[]};type Contract={id:string;provider_name:string};type Policy={id:string;insurance_type:string};type Asset={id:string;name:string};type Liability={id:string;name:string};
export default function CostCentersPage(){const qc=useQueryClient();const centers=useQuery({queryKey:['cost-centers'],queryFn:()=>apiGet<Center[]>('/api/v1/cost-centers')});const contracts=useQuery({queryKey:['contracts'],queryFn:()=>apiGet<Contract[]>('/api/v1/contracts')});const policies=useQuery({queryKey:['insurance'],queryFn:()=>apiGet<Policy[]>('/api/v1/insurance')});const assets=useQuery({queryKey:['assets'],queryFn:()=>apiGet<Asset[]>('/api/v1/assets')});const liabilities=useQuery({queryKey:['liabilities'],queryFn:()=>apiGet<Liability[]>('/api/v1/liabilities')});const [selected,setSelected]=useState('');const summary=useQuery({queryKey:['cost-center-summary',selected],queryFn:()=>apiGet<Summary>('/api/v1/cost-centers/'+selected+'/summary'),enabled:!!selected});const [name,setName]=useState('');const create=useMutation({mutationFn:()=>apiMutate<{id:string}>('/api/v1/cost-centers','POST',{name,center_type:'life_area',metadata:{}}),onSuccess:r=>{setName('');setSelected(r.id);qc.invalidateQueries({queryKey:['cost-centers']})}});const [link,setLink]=useState({entity_type:'contract',entity_id:'',allocation_percentage:'100'});const options=link.entity_type==='contract'?contracts.data?.map(x=>({id:x.id,label:x.provider_name})):link.entity_type==='insurance_policy'?policies.data?.map(x=>({id:x.id,label:x.insurance_type})):link.entity_type==='asset'?assets.data?.map(x=>({id:x.id,label:x.name})):liabilities.data?.map(x=>({id:x.id,label:x.name}));const addLink=useMutation({mutationFn:()=>apiMutate('/api/v1/cost-center-links','POST',{cost_center_id:selected,...link}),onSuccess:()=>{qc.invalidateQueries({queryKey:['cost-centers']});qc.invalidateQueries({queryKey:['cost-center-summary',selected]})}});return <><PageHeader title="Centros de coste" description="Agrupa contratos, pólizas, activos y deuda por vivienda, coche, familia, proyecto u otra área. Las asignaciones parciales se respetan."/><div className="grid gap-4 xl:grid-cols-[340px_1fr]"><div className="space-y-4"><Card><h2 className="font-bold">Nuevo centro</h2><form className="mt-3 flex gap-2" onSubmit={(e:FormEvent)=>{e.preventDefault();create.mutate()}}><input className="fin-input" value={name} onChange={e=>setName(e.target.value)} placeholder="Ej. Vivienda"/><button className="fin-button">Crear</button></form></Card><Card><h2 className="font-bold">Centros</h2><div className="mt-3 space-y-2">{centers.data?.length?centers.data.map(c=><button key={c.id} className={'w-full rounded-xl p-3 text-left '+(selected===c.id?'bg-[var(--brand-soft)]':'bg-[var(--surface-2)]')} onClick={()=>setSelected(c.id)}><strong>{c.name}</strong><div className="text-xs text-[var(--muted)]">{c.links.length} vínculo(s)</div></button>):<EmptyState>Sin centros.</EmptyState>}</div></Card></div><div className="space-y-4">{!selected?<EmptyState>Selecciona un centro.</EmptyState>:<><Card><h2 className="font-bold">Vincular coste o referencia</h2><form className="mt-3 grid gap-2 md:grid-cols-3" onSubmit={(e:FormEvent)=>{e.preventDefault();addLink.mutate()}}><select className="fin-input" aria-label="Tipo de entidad" value={link.entity_type} onChange={e=>setLink({...link,entity_type:e.target.value,entity_id:''})}><option value="contract">Contrato</option><option value="insurance_policy">Póliza</option><option value="asset">Activo</option><option value="liability">Deuda</option></select><select className="fin-input" aria-label="Entidad a vincular" value={link.entity_id} onChange={e=>setLink({...link,entity_id:e.target.value})}><option value="">Elemento…</option>{options?.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select><input className="fin-input" type="number" min="0" max="100" step=".1" value={link.allocation_percentage} onChange={e=>setLink({...link,allocation_percentage:e.target.value})}/><button className="fin-button md:col-span-3">Vincular</button></form></Card><Card><h2 className="font-bold">Resumen</h2>{summary.error?<ErrorState error={summary.error}/>:summary.data&&<div className="mt-4 grid gap-4 sm:grid-cols-2"><div><div className="text-xs text-[var(--muted)]">Gasto observado vinculado</div><strong className="text-xl"><Money value={summary.data.observed_linked_spend}/></strong></div><div><div className="text-xs text-[var(--muted)]">Compromisos anuales</div><strong className="text-xl"><Money value={summary.data.annual_linked_commitments}/></strong></div><div><div className="text-xs text-[var(--muted)]">Valor de activos asociado</div><strong><Money value={summary.data.reference_asset_value}/></strong></div><div><div className="text-xs text-[var(--muted)]">Deuda asociada</div><strong><Money value={summary.data.reference_debt}/></strong></div></div>}</Card></>}</div></div></>}
